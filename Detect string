user_input=input()
y=[]
for i in range(len(user_input)):
    if user_input[i] == '"':
        y.append(i)

    
for j in range(0,len(y),2):
    start = y[j]
    end = y[j+1]+1
    print(user_input[start: end])
